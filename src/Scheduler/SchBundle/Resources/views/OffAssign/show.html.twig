<h1>OffAssign</h1>

<table class="record_properties table table-condensed">
  <tbody>
  <tr>
    <th>Id</th>
    <td>{{ entity.id }}</td>
  </tr>
  </tbody>
</table>

<ul class="record_actions">
  <li>
    <a href="{{ path('offassign') }}">
      Back to the list
    </a>
  </li>
  <li>
    <a href="{{ path('offassign_edit', { 'id': entity.id }) }}">
      Edit
    </a>
  </li>
  <li>
    <form action="{{ path('offassign_delete', { 'id': entity.id }) }}" method="post">
      {{ form_widget(delete_form) }}
      <button type="submit">Delete</button>
    </form>
  </li>
</ul>
