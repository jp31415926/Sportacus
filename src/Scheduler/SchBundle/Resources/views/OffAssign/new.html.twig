{% extends 'SchedulerBundle::layout.html.twig' %}
{% block title %}Create Official Assignment{% endblock %}

{% block body %}

  <h1>OffAssign creation</h1>

  {{ form_start(form, {'action': path('offassign_create'), 'attr': {'class': 'contact'} }) }}
    {{ form_widget(form) }}
    <p>
      <button type="submit" class="btn btn-primary btn-large">Create</button>
    </p>
  {{ form_end(form) }}
  <p><a href="{{ path('offassign') }}" class="btn btn-default"><i class="glyphicon glyphicon-backward"></i> Back to the list</a></p>

{% endblock %}
