{% extends 'SchedulerBundle::layout.html.twig' %}
{% set title='Create RefPntsMap' %}
{% block title %}{{ title }}{% endblock %}

{% block body %}
  <h1>{{ title }}</h1>

  {{ form_start(form, {'action': path('refpntsmap_create'), 'attr': {'class': 'contact'} }) }}
    {{ form_widget(form) }}
    <p>
      <button type="submit" class="btn btn-primary btn-large">Create</button>
    </p>
  {{ form_end(form) }}
  <p><a href="{{ path('refpntsmap') }}" class="btn btn-default"><i class="glyphicon glyphicon-backward"></i> List</a></p>

{% endblock %}
