id,Number,Date,Time,Team1,Team2,Division,Length,TSLength,Location,Region,Ref1,Ref2,Ref3,Ref4,Ref5
{% for r in data %}
  {{ r.id }},{{ r.number }},{{ r.date|date('Y-m-d') }},{{ r.time|date('G:i') }},{{ r.team1.name }},{{ r.team2.name }},{{ r.agegroup }},{{ r.length }},{{ r.timeslotlength }},{{ r.location }},{% if r.region %}{{ r.region.name }}{% endif %},{{ r.ref1 }},{{ r.ref2 }},{{ r.ref3 }},{{ r.ref4 }},{{ r.ref5 }}
  r.ref1.email }},{{ r.ref2.email }},{{ r.ref3.email }},{{ r.ref4.email }},{{ r.ref5.email }}
{% endfor %}
